# 红利 MA30 停车策略 · 自动提醒系统

每个交易日北京时间 21:00 自动抓数、算信号、记账、推送到手机。非交易日静默。

面板：https://binbin1555.github.io/hongli-ma30/

---

## 这套系统在做什么

在中证红利指数上执行「跌了买一档、涨了卖一档」的分档波段，不在股票里的钱放债券基金。

| | |
|---|---|
| 信号源 | 中证红利**全收益**指数 H00922（不能用价格指数 000922） |
| 均线 | MA30 = 最近 30 个交易日收盘价的算术平均，含当日 |
| 买入 | 收盘 < MA30 × 0.97 → 档位 +1 |
| 卖出 | 收盘 > MA30 × 1.02 → 档位 −1 |
| 档位 | 0 / 1 / 2 / 3 / 4 份，对应仓位 0 / 25% / 50% / 75% / 100% |
| 一天最多 | 动一档 |
| 执行 | T 日收盘出信号 → T+1 日收盘成交 |
| 下单金额 | 目标市值法：买 X =（w×S − V）/（1 + w×c），卖 X =（V − w×S）/（1 − w×c） |
| 实际买卖 | 红利ETF 515180（易方达，跟踪中证红利 000922） |
| 下单以哪个数为准 | **面板计算器**（填你券商 App 的真实数字）。推送和横幅里的金额是按本金推算的**估算值** |
| 闲钱 | 债券基金，业绩基准中证全债 H11001 |

买卖阈值刻意不对称（买 −3%、卖 +2%）。一轮完整来回的毛利空间是 1.02÷0.97 = **+5.15%**，这是策略对抗均线下行的全部缓冲垫。**不要对称化。**

---

## 架构

```
cron-job.org ──每交易日 21:00 HTTP──> Cloudflare Worker（唯一会跑代码的组件）
                                          │
                                          ├─ 深交所日历：今天开市吗？不开市就静默退出
                                          ├─ 中证接口：抓 H00922 + H11001
                                          ├─ ETF 行情：腾讯→新浪→东财三源依次尝试（换算股数）
                                          ├─ 10 项校验，任何一项不过就告警且不写入
                                          ├─ 算 MA30 / 信号 / 档位
                                          ├─ git commit 到本仓库 ← 这就是不可更改的账本
                                          └─ Bark 推送到手机

手机 ──> GitHub Pages（纯静态页面）──读──> 仓库里的 data/*.json
```

**手机永远不需要访问 Cloudflare。** 前端是纯静态的，直接读仓库里的 JSON，所以国内访问只依赖 GitHub Pages。

**仓库里没有任何金额。** 账本只记日期、指数收盘价、档位从几变到几。金额由本金实时推导出来——因为金额本来就是「档位 + 总资产」的确定函数。所以这个仓库可以公开而不泄露你的资金规模。本金存在两个地方：Worker 的密钥（用于生成推送文案）和你手机浏览器的 localStorage（用于显示面板）。两边用的是同一份 `shared/strategy.js`，算出来必然一致。

**账本为什么改不了。** 每天一次 git commit，git 是内容寻址的：改动任何一条历史记录都会让它之后所有 commit 的哈希全部变化，在 GitHub 上一眼可见。这比在代码里写个「已锁定」标志可靠得多。

---

## 目录

```
index.html            面板（纯静态，GitHub Pages 的入口）
shared/strategy.js    策略核心纯函数，Worker 和面板共用同一份
worker/src/index.js   Cloudflare Worker
wrangler.toml         Worker 配置（机密不在这里）
data/state.json       当前状态：档位、待执行挂单、指数点位、校验结果
data/ledger.json      账本，只增不改
data/series.json      行情序列，供画图和重放
calendar/2026.json    交易日历，每月自动刷新
audit/YYYY-MM-DD.json 每日运行快照，用于事后追溯
tests/                回归测试
```

---

## 部署

### 1. 发布仓库

已完成：https://github.com/binbin1555/hongli-ma30 （公开，默认分支 `master`）

### 2. 打开 GitHub Pages

仓库 Settings → Pages → Source 选 `Deploy from a branch`，Branch 选 **`master`**（本仓库的默认分支）、目录选 `/ (root)` → Save。
一两分钟后 https://binbin1555.github.io/hongli-ma30/ 就能打开。

### 3. 建一个 GitHub Token

GitHub Settings → Developer settings → Personal access tokens → **Fine-grained tokens** → Generate new token：

- Repository access：只选 `binbin1555/hongli-ma30` 这一个仓库
- Permissions → Repository permissions → **Contents: Read and write**（其他都不要给）
- 有效期建议选 1 年，到期前 GitHub 会发邮件提醒

### 4. 部署 Worker

Cloudflare Dashboard → Workers & Pages → Create → Connect to Git → 选这个仓库。
构建配置留空即可（wrangler.toml 已经写好）。

然后设置 4 个机密（Settings → Variables and Secrets → Add，类型选 **Secret**）：

| 名称 | 值 |
|---|---|
| `GITHUB_TOKEN` | 上一步生成的 token |
| `BARK_KEY` | Bark App 里那串设备 key |
| `RUN_TOKEN` | 自己编一串长口令，用来保护 /run 端点 |
| `PRINCIPAL` | 本金，例如 `1000000` |

Worker 会自动向 GitHub 查询默认分支，`main` 还是 `master` 都不用管。

**这四个值不要写进仓库里的任何文件。**

### 5. 配 cron-job.org

建**三个**任务：

| 任务 | URL | 频率 |
|---|---|---|
| 每日运行 | `https://<你的worker地址>/run?token=<RUN_TOKEN>` | 每天 21:00（时区选 Asia/Shanghai） |
| 盘中提醒 | `https://<你的worker地址>/remind?token=<RUN_TOKEN>` | 每天 14:30 |
| 每月刷新日历 | `https://<你的worker地址>/calendar/refresh?token=<RUN_TOKEN>` | 每月 1 日 03:00 |

**盘中提醒**只读不写：只在「今天正好是某笔挂单的执行日」时推一条，其余情况静默。
它防的是 T 日晚上看到通知、T+1 日却忘了下单——漏做超过一天，账本会继续往前走，
实盘就落后了。

每日任务设成周一到周五就够了 —— 周末交易所日历本来就返回非交易日，Worker 会静默退出。遇到调休上班的周六，日历会正确标记为交易日，所以**建议还是设成每天跑**，让日历去判断，而不是让 cron 去判断。

### 6. 首次验证

浏览器打开 `https://<你的worker地址>/health`，应该返回当前状态。
然后手动跑一次 `https://<你的worker地址>/run?token=<RUN_TOKEN>&force=1`，检查：手机收到推送、仓库多了一个 commit、面板数字更新。

---

## 10 项校验

任何一项 `fatal` 失败都会**阻止写入**并推一条带原因的告警。绝不静默失败。

| # | 检查 | 防的是什么 |
|---|---|---|
| 1 | 深交所日历确认今天开市 | 节假日、调休误推送 |
| 2 | 中证今日数据已到位 | 日历说开市但数据没出，宁可告警也不用旧数据算 |
| 3 | 日期严格递增，且每一天都在官方交易日历里 | 接口塞进不存在的交易日 |
| 4 | 幽灵行已清除 | 接口在区间起点补一行等于次日收盘价的假数据 |
| 5 | 涨跌幅与收盘价逐行对账 | 数据串行、错位 |
| 6 | 单日波动在 ±11% 内 | 明显的脏数据 |
| 7 | MA30 有满 30 个交易日 | 用不足 30 天的均线算出错误信号 |
| 8 | ETF 报价日期与指数日期一致 | 行情源会返回当日**盘中实时**价，用它换算股数会错 |
| 9 | state 档位与账本一致 | 提交只写了一半、或有人手改 state.json 导致状态与账本脱节 |
| 10 | 账本自审：日期与档位链完整 | **最重要的一项**：账本里只要有一天不在行情序列里，重放会静默跳过，后面每一笔金额都错但界面看不出异常 |

第 8 项非致命 —— 失败时只是不给参考股数，仍然给金额。

---

## 回归测试

```bash
npm test            # 回归测试（含文案测试）
npm run wording     # 只跑文案：打印每种状态的真实句子
npm run sweep       # 状态空间穷举（2808 组）
npm run check:sources   # 三个外部数据源体检
npm run check:deploy -- <worker 地址>   # 线上跑的是不是本地这一版
```

`npm test` 把 `shared/strategy.js` 的重放结果和独立的 Python 回测引擎逐笔对账
（三段历史、最多 62 笔交易路径，误差 < 0.01 元），另外覆盖：信号阈值边界、档位推进、
目标市值法公式、MA30 边界、北京日期换算、交易日推算、计算器方向不变量、补齐落点、
提示边界、校验体系逐项故障注入、状态文件校验、前端模块版本戳。

`npm run sweep` 穷举 5 档位 × 8 价格位置 × 3 挂单形态 × 3 资产规模 × 9 持仓占比，
检查「0 档不提卖出、满档不提买入、成交后精确落在目标仓位」等九项不变量。

### 执行时点：收盘，不是「收盘前」

说明文档 3.4「T 日收盘产生信号 → T+1 日收盘执行」、7 节「成交价用当日收盘价」——
账本按**当日收盘价**记账。所以文案一律说「收盘成交」，不许松成「收盘前下单」：
后者字面上允许你上午十点就买，那天振幅 2%，你的成交价就和账本差 2%，
而这笔误差回测里根本没算过。

对应的可执行动作由  的  统一给出，
推送和面板共用：**请在尾盘（14:50 之后）下单**。

> hongli-ma30@1.0.0 wording
> node tests/wording.mjs


================ 「下一次操作」与触发横幅 ================

【空仓等待买入】
  卡片  距离下一次买入，还需中证红利跌 3.00%
  副行  跌到 5,820 触发　·　买入第 1 份（仓位 25%） ⏎ 触发当日 21:00 挂单，再下一个交易日收盘成交 —— 具体日期要到触发后才定得下来，届时这张卡片会直接写出来。 ⏎ 百分比按 2026-09-10（周四）收盘 6,000 计，盘中实时价请自行对照上面的触发点位。

【半仓等待（买卖都还远）】
  卡片  距离下一次卖出，还需中证红利涨 2.00%
  副行  涨到 6,120 触发　·　卖出第 2 份（仓位降到 25%） ⏎ 反向也可能先到：跌 3.00% 到 5,820 则改为买入第 3 份。 ⏎ 触发当日 21:00 挂单，再下一个交易日收盘成交 —— 具体日期要到触发后才定得下来，届时这张卡片会直接写出来。 ⏎ 百分比按 2026-09-10（周四）收盘 6,000 计，盘中实时价请自行对照上面的触发点位。

【满仓等待卖出】
  卡片  距离下一次卖出，还需中证红利涨 2.00%
  副行  涨到 6,120 触发　·　卖出第 4 份（仓位降到 75%） ⏎ 触发当日 21:00 挂单，再下一个交易日收盘成交 —— 具体日期要到触发后才定得下来，届时这张卡片会直接写出来。 ⏎ 百分比按 2026-09-10（周四）收盘 6,000 计，盘中实时价请自行对照上面的触发点位。

【已跌破买入线但还没出挂单】
  卡片  9 月 10 日（昨天，周四）收盘已跌破买入线，但还没生成挂单
  副行  2026-09-10（周四）收盘 5,760 已在买入线 5,820 之下　·　按规则应挂单买入第 2 份 ⏎ 若 2026-09-10 21:00 的运行还没跑完，稍等再刷新；若已过 2026-09-10 22:00 仍是这样，请看下方「系统自检」。

【已涨破卖出线但还没出挂单】
  卡片  9 月 10 日（昨天，周四）收盘已涨破卖出线，但还没生成挂单
  副行  2026-09-10（周四）收盘 6,180 已在卖出线 6,120 之上　·　按规则应挂单卖出第 3 份 ⏎ 若 2026-09-10 21:00 的运行还没跑完，稍等再刷新；若已过 2026-09-10 22:00 仍是这样，请看下方「系统自检」。

【挂单·执行日就是今天】
  卡片  9 月 11 日（今天，周五）收盘买入第 2 份
  副行  档位 1/5 → 2/5，目标仓位 50% ⏎ 估算约 243,183 元（按本金推算，仅供估量） ⏎ 信号日 2026-09-10　·　9 月 11 日尾盘必须完成 ⏎ 请在尾盘（14:50 之后）下单 —— 账本按当日收盘价记账，越贴近收盘，实盘与账本的偏差越小
  横幅  触发 · 9 月 11 日（今天，周五）收盘执行　档位 1/5 → 2/5
        买入第 2 份　目标仓位 50%
        估算约 243,183 元，按本金推算，下单以下方计算器为准。 ⏎ 此横幅仅供你确认已下单。无论是否点击，系统都已按规则把这笔操作写入不可更改的记录。

【挂单·执行日在将来】
  卡片  9 月 14 日（周一）收盘买入第 2 份
  副行  档位 1/5 → 2/5，目标仓位 50% ⏎ 估算约 243,183 元（按本金推算，仅供估量） ⏎ 信号日 2026-09-11　·　执行日 2026-09-14（周一），距今 3 天，中间的休市日不用操作 ⏎ 请在尾盘（14:50 之后）下单 —— 账本按当日收盘价记账，越贴近收盘，实盘与账本的偏差越小
  横幅  触发 · 9 月 14 日（周一）收盘执行　档位 1/5 → 2/5
        买入第 2 份　目标仓位 50%
        估算约 243,183 元，按本金推算，下单以下方计算器为准。 ⏎ 此横幅仅供你确认已下单。无论是否点击，系统都已按规则把这笔操作写入不可更改的记录。

【挂单·执行日已过】
  卡片  本应在 9 月 8 日（周二）收盘买入第 2 份
  副行  档位 1/5 → 2/5，目标仓位 50% ⏎ 估算约 243,183 元（按本金推算，仅供估量） ⏎ 信号日 2026-09-07　·　执行日 2026-09-08（周二）已过，账本当日 21:00 已按规则记上，实盘请尽快补做 ⏎ 请在尾盘（14:50 之后）下单 —— 账本按当日收盘价记账，越贴近收盘，实盘与账本的偏差越小
  横幅  触发 · 9 月 8 日（周二）收盘执行 · 已过期，请尽快补做　档位 1/5 → 2/5
        买入第 2 份　目标仓位 50%
        估算约 243,183 元，按本金推算，下单以下方计算器为准。 ⏎ 此横幅仅供你确认已下单。无论是否点击，系统都已按规则把这笔操作写入不可更改的记录。

【挂单·算不出执行日】
  卡片  下一个交易日（交易日历暂时取不到，给不出具体日期）收盘买入第 2 份
  副行  档位 1/5 → 2/5，目标仓位 50% ⏎ 估算约 243,183 元（按本金推算，仅供估量） ⏎ 信号日 2099-01-02　·　交易日历暂时取不到，给不出执行日期 ⏎ 请在尾盘（14:50 之后）下单 —— 账本按当日收盘价记账，越贴近收盘，实盘与账本的偏差越小
  横幅  触发 · 下一个交易日（交易日历暂时取不到，给不出具体日期）收盘执行　档位 1/5 → 2/5
        买入第 2 份　目标仓位 50%
        估算约 243,183 元，按本金推算，下单以下方计算器为准。 ⏎ 此横幅仅供你确认已下单。无论是否点击，系统都已按规则把这笔操作写入不可更改的记录。

【挂单·卖出方向】
  卡片  9 月 11 日（今天，周五）收盘卖出第 3 份
  副行  档位 3/5 → 2/5，目标仓位 50% ⏎ 估算约 267,421 元（按本金推算，仅供估量） ⏎ 信号日 2026-09-10　·　9 月 11 日尾盘必须完成 ⏎ 请在尾盘（14:50 之后）下单 —— 账本按当日收盘价记账，越贴近收盘，实盘与账本的偏差越小
  横幅  触发 · 9 月 11 日（今天，周五）收盘执行　档位 3/5 → 2/5
        卖出第 3 份　目标仓位 50%
        估算约 267,421 元，按本金推算，下单以下方计算器为准。 ⏎ 此横幅仅供你确认已下单。无论是否点击，系统都已按规则把这笔操作写入不可更改的记录。

【挂单·清空到 0 档】
  卡片  9 月 11 日（今天，周五）收盘卖出第 1 份
  副行  档位 1/5 → 0/5，目标仓位 0% ⏎ 估算约 270,807 元（按本金推算，仅供估量） ⏎ 信号日 2026-09-10　·　9 月 11 日尾盘必须完成 ⏎ 请在尾盘（14:50 之后）下单 —— 账本按当日收盘价记账，越贴近收盘，实盘与账本的偏差越小
  横幅  触发 · 9 月 11 日（今天，周五）收盘执行　档位 1/5 → 0/5
        卖出第 1 份　目标仓位 0%
        估算约 270,807 元，按本金推算，下单以下方计算器为准。 ⏎ 此横幅仅供你确认已下单。无论是否点击，系统都已按规则把这笔操作写入不可更改的记录。

【落后账本·漏做两笔买入】
  卡片  你有 2 笔操作还没做，需一次性买入 499,989 元补到位
  副行  实盘 0/5 档，低于账本的 2/5 档 ⏎ 漏掉的记录：2026-07-17 买0→1　2026-07-31 买1→2（2 笔合起来只需这一次调仓） ⏎ 补完之后才会回到正常的信号节奏。详见下方「每份该是多少钱」。

【落后账本·来回三笔只需补一次】
  卡片  你有 3 笔操作还没做，需一次性买入 249,997 元补到位
  副行  实盘 0/5 档，低于账本的 1/5 档 ⏎ 漏掉的记录：2026-07-17 买0→1　2026-07-31 买1→2　2026-08-14 卖2→1（3 笔合起来只需这一次调仓） ⏎ 补完之后才会回到正常的信号节奏。详见下方「每份该是多少钱」。

【实盘超前·账本里没有对应记录】
  卡片  你填的实盘仓位和账本对不上，需一次性卖出 500,000 元补到位
  副行  实盘 2/5 档，高于账本的 0/5 档 ⏎ 账本里没有对得上的漏做记录 —— 可能是持仓涨跌跨过了档位边界，也可能是计算器里的数字填错了。 ⏎ 补完之后才会回到正常的信号节奏。详见下方「每份该是多少钱」。

【挂单在身·实盘还落后】
  卡片  你有 2 笔操作还没做，需一次性买入 749,975 元补到位
  副行  实盘 0/5 档，低于账本的 3/5 档 ⏎ 漏掉的记录：2026-07-17 买0→1　2026-07-31 买1→2（2 笔合起来只需这一次调仓） ⏎ 补完之后才会回到正常的信号节奏。详见下方「每份该是多少钱」。
  横幅  触发 · 9 月 11 日（今天，周五）收盘执行　档位 2/5 → 3/5
        买入第 3 份　目标仓位 75%
        估算约 253,488 元，按本金推算，下单以下方计算器为准。 ⏎ 按你填的实盘数字应为 买入 749,975 元 —— 以这个为准。上面是按本金推算的理论值。 ⏎ 此横幅仅供你确认已下单。无论是否点击，系统都已按规则把这笔操作写入不可更改的记录。

================ Bark 推送：执行日措辞 ================

日历内 241 个信号日，其中 50 个的执行日不是第二天（周末与节假日）。
  信号 2026-01-05 → 执行 2026-01-06
    标题 …1 月 6 日（明天，周二）收盘执行
    正文 执行日 2026-01-06（周二）收盘成交
  信号 2026-01-09 → 执行 2026-01-12
    标题 …1 月 12 日（周一）收盘执行
    正文 执行日 2026-01-12（周一）收盘成交　·　距出信号 3 天，中间的休市日不用操作
  信号 2026-12-25 → 执行 2026-12-28
    标题 …12 月 28 日（周一）收盘执行
    正文 执行日 2026-12-28（周一）收盘成交　·　距出信号 3 天，中间的休市日不用操作
  执行日取不到 → 标题 …下一个交易日（交易日历暂时取不到，给不出具体日期）收盘执行
    正文 交易日历暂时取不到，给不出执行日期。请到面板核对后再下单。

================ 源码里的写死相对词 ================

  扫过 4 行含相对时间词的代码（注释已排除）

✓ 全部状态文案通过 会拦截任何  的写法。

### 日期措辞的硬规矩

**凡出现相对时间词的那一句，要么同句给出具体日期，要么明说「给不出具体日期」。**

相对词只允许待在日期后面的括号里做定位 —— 把括号整个删掉，句子必须依然完全正确。

```
9 月 14 日（周一）收盘执行          ✓ 删掉括号仍然对
9 月 11 日（今天，周五）收盘前      ✓ 同上
明日收盘执行                        ✗ 周五出的信号执行日是下周一
今天收盘前下单                      ✗ 放了一夜的页面上，「今天」是昨天
下一个交易日收盘执行                ✗ 不看日历不知道是哪天
下一个交易日（交易日历暂时取不到，给不出具体日期）  ✓ 明说给不出
```

措辞统一由 `shared/strategy.js` 的 `dayLabel(日期, 北京今天)` 生成，
推送和面板共用同一个函数 —— 各写各的迟早会分家。
`npm run wording` 会同时扫渲染出的真句子和源码里的字符串字面量，违规就报错。

`npm run wording` 跑的是 `index.html` 里的真代码 —— 把 `<script type="module">`
整段抠出来配 DOM 假件后 import 进来，穷举 15 种状态，打印「下一次操作」卡片、
触发横幅、Bark 推送的**真实文案**，并断言：不出现 `undefined`/`NaN`，
卡片和横幅不说两个时点、不说两个方向，「N 笔」和账本对得上，
以及推送里的「明日」当且仅当执行日真是第二天时才出现（2026 年 241 个信号日逐个验，
其中 50 个的执行日因周末和节假日不是第二天）。**改了任何提示文案都要跑一遍并读输出。**

`npm run check:deploy` 拿本地源码算出 `BUILD` 指纹，去比对线上 `/health` 返回的那个。
**Cloudflare 的构建记录只能告诉你「某次构建成功了」，不能告诉你此刻在跑的是哪一版** ——
推完代码用它确认一句，比翻控制台快，也比翻控制台准。地址不写进仓库（仓库是公开的，
而 `/health` 不鉴权），每次从命令行传，或设环境变量 `WORKER_URL`。

`npm run check:sources` 值得偶尔手动跑 —— 两轮审查里最有价值的发现，
就是数据源会悄悄失效而系统只在日志里留一行。

**改动 `shared/strategy.js` 之后一定要跑前两个。**

---

## 需要留意的事

**这不是一个能长期跑赢中证红利指数的策略。** 拉到 20 年它大概率跑输，而且会输很多。它的定位是「停车位」：在等待大熊市底部期间保住本金、少亏钱。

**近十年的漂亮成绩里，约 89% 来自债券腿而不是择时。** 十年期国债收益率已经从 3.9% 降到 1.68%，这条腿的贡献不可能再复制。2025 年中证全债滚动一年收益一度是 −0.04%。

**它真正怕的不是「跌得深」，是「跌得慢、跌得久、中间不给反弹」。** 历史上唯一让这套规则彻底失败的是 2008 年那种长期阴跌。判断标准不看跌幅，看有没有出现 5% 以上的像样反弹。

**决定成败的头号变量是持有期内指数的最大回撤。** 回撤小于 25% 时基本必成，超过 50% 时基本必败。

**漏做了怎么办。** 系统按规则记账，不管你实际有没有下单（这是设计如此）。如果你漏做了几天，
实盘就会落后账本。面板的计算器会检测到并直接给出「一次补齐到账本 N/5 档」的金额——
注意它旁边「只走一档」的数字在落后两档时只有实际需要量的一半，别拿错。

**账本是理论口径。** 股票腿用全收益指数、闲钱用全债指数推算，不追踪你实盘的零头差异。实盘会有 ETF 跟踪误差、分红税负、冲击成本，**实际表现通常低于面板显示的理论值**。
面板上「每份该是多少钱」是个**独立的计算器**：填入你券商 App 里的真实「可用资金」和「已持有红利」，它会按这两个数字反推你在第几档、下一步该动多少钱。在那里填什么都不会影响操作提醒和交易记录——账户盈亏和账本金额只由**本金**推导。

**状态文件损坏时页面会整页停止渲染**，而不是显示一个看似正常的错数字。
Worker 读到损坏状态也会直接告警退出，不基于它算信号。

**退出规则尚未定义。** 这套策略是为了等一个大熊市底部、然后全部换成创业板 ETF 而存在的。换仓的触发规则目前还没定，而这个决定对最终结果的影响比这里所有参数加起来都大。

---

## 日常维护

**Worker 会自己往仓库提交。** 每个交易日 21:00 它会写 `data/`、`calendar/`、`audit/`，所以远端经常比你本地新。GitHub Desktop 弹「Newer commits on remote」是正常的，不是故障。

改代码的固定动作：

```
Fetch  →  Pull origin  →  改代码  →  Commit  →  Push origin
```

**不要在本地手动改这三个目录**，它们属于 Worker，改了才会真冲突：

| 目录 | 归谁 |
|---|---|
| `data/` `calendar/` `audit/` | Worker 写，只读 |
| `index.html` `worker/` `shared/` `tests/` | 你改 |

改完 `shared/strategy.js` 要跑两条命令：

```bash
npm run stamp   # 更新前端模块的版本指纹，否则浏览器会用缓存的旧模块导致白屏
npm test        # 和独立的 Python 回测引擎逐笔对账 + 全部单元测试
```

忘了 stamp 的话 `npm test` 会直接报错提醒你。

---

## 数据来源

| 用途 | 来源 |
|---|---|
| 信号与回测 | 中证指数有限公司 `csindex.com.cn/csindex-home/perf/index-perf` |
| 交易日历 | 深圳证券交易所 `szse.cn/api/report/exchange/onepersistenthour/monthList` |
| ETF 参考价 | 腾讯 `qt.gtimg.cn` → 新浪 `hq.sinajs.cn` → 东财 `push2.eastmoney.com`，三源依次尝试 |

交易日历用交易所一手数据，实测 12 个月份（含 3 次春节、2 次国庆、3 次劳动节调休）与中证数据零差异。
每年的日历在国务院公布节假日安排后自动出现，不需要维护硬编码的假期表。

---

*本系统未经实盘长期验证。历史回测结果不构成对未来表现的承诺。*
