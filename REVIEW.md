# 代码审查计划与结论

审查对象：`binbin1555/hongli-ma30` 全系统
审查日期：2026-09-10（首次真实运行当晚）
代码规模：策略核心 295 行 ／ Worker 722 行 ／ 前端 870 行 ／ 测试 294 行

---

## 审查方法

这套系统的特殊之处在于：**它每天自动运行、自动写账本，出错时你不一定看得见。**
所以审查不按"读一遍代码找语法问题"的路子走，而是围绕三个问题组织：

1. **会不会算错？** —— 数值正确性，用穷举和对账验证，不靠肉眼
2. **会不会静默失败？** —— 失败路径是否都有告警，还是悄悄跳过
3. **时间走下去会不会坏？** —— 跨年、数据增长、凭证过期、数据源变更

每条结论必须有可复现的证据（测试输出、真实接口响应、审计快照），不接受"看起来没问题"。

严重度分级：

| 级别 | 含义 |
|---|---|
| **P0** | 会导致错误的买卖金额或错误的账本记录，直接造成资金损失 |
| **P1** | 会导致漏推送、错推送或静默失败，你不会及时知道出了问题 |
| **P2** | 显示错误或体验问题，不影响决策正确性 |
| **P3** | 长期隐患，现在不影响，将来某天会发作 |

---

## 审查清单

### A. 策略核心 `shared/strategy.js`

| # | 检查项 | 方法 |
|---|---|---|
| A1 | 信号阈值边界（恰好等于 0.97/1.02 时不触发） | 单元测试 |
| A2 | 档位推进封顶封底、一天最多一档 | 穷举 |
| A3 | 目标市值法公式与文档 3.5 逐字一致 | 手算对账 |
| A4 | `replay` 与独立 Python 引擎逐笔一致 | 三段历史对账 |
| A5 | `calcStep` 方向不变量（买入永不算出卖出） | 穷举 2800+ 组 |
| A6 | `calcCatchUp` 成交后精确落在目标仓位 | 穷举 2500+ 组 |
| A7 | `nextMove` 边界（0档不提卖、4档不提买） | 穷举 |
| A8 | `beijingDate` 跨时区、跨午夜、跨年 | 定点测试 |
| A9 | `nextTradingDay` 跨周末、跨长假、跨年 | 真实日历遍历 |
| A10 | 非法输入（NaN／负数／零／超界档位）全部安全返回 | 边界测试 |
| A11 | 浮点累积误差是否会随时间放大 | 长序列重放 |

### B. Worker 主流程 `worker/src/index.js`

| # | 检查项 | 方法 |
|---|---|---|
| B1 | T+1 执行时序：信号日与成交日不能错位 | 状态机推演 |
| B2 | 幂等：同日重跑、force 重跑不产生重复账本 | 代码路径分析 |
| B3 | 漏跑一天后的追赶行为是否正确且有告警 | 场景推演 |
| B4 | 跨年时 `late` 判定、日历查询是否失效 | 边界分析 |
| B5 | 提交原子性：部分文件写入失败会怎样 | Git Data API 路径分析 |
| B6 | 校验失败时确实不写入（而非写了一半） | 代码路径分析 |
| B7 | 本金缺失时的降级行为 | 分支分析 |

### C. 数据源与清洗

| # | 检查项 | 方法 |
|---|---|---|
| C1 | 中证接口：分页截断、幽灵行、节假日补行 | 真实请求验证 |
| C2 | 深交所日历：可用性、跨年续期 | 真实请求验证 |
| C3 | 东方财富 ETF：**首次运行已失败，需定位** | 真实请求 + Worker 环境模拟 |
| C4 | 三个数据源任一挂掉时的降级行为 | 故障注入分析 |
| C5 | 重试与超时是否会撑爆 cron 的 30 秒上限 | 耗时估算 |

### D. 校验体系

| # | 检查项 | 方法 |
|---|---|---|
| D1 | 10 项校验逐条是否真能拦住它声称拦的问题 | 故障注入 |
| D2 | 是否存在"永远通过"的假校验 | 逐条推演 |
| D3 | fatal 与非 fatal 的划分是否合理 | 逐条评估 |
| D4 | 校验失败的告警是否一定送达 | 与 E 合并 |

### E. 推送送达

| # | 检查项 | 方法 |
|---|---|---|
| E1 | Bark 失败时是否有兜底（目前疑似静默吞掉） | 代码路径分析 |
| E2 | cron 打不通 Worker 时你能否知道 | 端到端分析 |
| E3 | 推送内容与面板显示是否可能不一致 | 同源性验证 |

### F. 前端

| # | 检查项 | 方法 |
|---|---|---|
| F1 | 各档位 × 各价格位置的文案正确性 | 状态穷举 |
| F2 | 计算器在各种输入下结果正确 | 穷举（已覆盖） |
| F3 | 计算器与提醒、账本的隔离性 | 代码路径 + 测试 |
| F4 | 模块缓存导致白屏的风险 | 版本戳机制验证 |
| F5 | 数据加载失败时的表现 | 故障注入 |
| F6 | 窄屏布局、横向溢出 | 实测 |

### G. 部署与运维

| # | 检查项 | 方法 |
|---|---|---|
| G1 | 密钥是否有泄漏路径（仓库、日志、HTTP 返回） | 全文检索 |
| G2 | 公开仓库是否真的不含金额 | 全文检索 |
| G3 | token 过期、日历断档的提醒机制 | 分支分析 |
| G4 | 端点鉴权是否完备 | 路由分析 |

### H. 长期演化

| # | 检查项 | 方法 |
|---|---|---|
| H1 | 跨年（2026→2027）全链路 | 边界推演 |
| H2 | 数据文件无限增长 | 容量估算 |
| H3 | 数据源接口变更的探测能力 | 校验覆盖分析 |

---

## 审查结论

一共发现 **8 处问题**，其中 2 处 P1、3 处 P2、3 处 P3。**7 处已修复并补测试，1 处评估后接受现状。**

### P1 · 会静默失败，你不会及时知道

**① ETF 报价源已在生产环境失效** — 已修复

首次真实运行（2026-09-10）的审计快照里，校验 #8 失败：
`ETF 报价获取失败：东方财富 515180 请求失败：HTTP 520`。
重试 3 次全败，净损耗 3.7 秒。这是股数提示的唯一来源，等于该功能已经不可用。

复现确认：从本机请求同一地址也被直接断开连接，不是 Cloudflare 出口的问题，
是东财 K 线接口本身不稳定。

修复：改为三源依次尝试 —— 腾讯 `qt.gtimg.cn` → 新浪 `hq.sinajs.cn` → 东财实时接口。
三个解析器都对着实盘数据验证过，给出一致结果（1.46 / 2026-09-10）。
腾讯作为首选是因为它自带明确时间戳，正好满足校验 #8 的日期比对。
三源全挂才返回 null，且降级路径会记进审计日志。

**② Bark 推送失败被静默吞掉** — 已修复

`bark()` 内部 `catch` 后返回错误对象，但 **9 处调用点无一检查返回值**。
推送是这套系统唯一的输出通道 —— 它失败等于系统失效，而你不会知道。
更麻烦的是：推送失败时，没法用推送来告诉你推送失败了。

修复：记录每次推送结果，写进 `state.json` 的 `push` 字段、审计快照和 HTTP 返回。
面板上失败时药丸变红显示「推送失败」，并飘一条醒目告警说明原因。
cron-job.org 的执行历史里也能看到。

### P2 · 一致性与显示

**③ 校验 #9 是假校验** — 已修复

原第 9 项断言「档位一天最多动一档」，但这正是 `nextTier` 的结构性质 ——
穷举 25 组证明它永远成立，这条校验永远通过，等于没有。

修复：改为校验 **state 档位与账本是否脱节**（`state.tier` 必须等于账本最后一笔的
`tierTo`）。这才是真实风险：提交只写了一半、或有人手改了 `state.json`。
已用故障注入验证它能抓到脱节。

**④ 校验计数与页面项数对不上** — 已修复

`state.json` 记 `total=8`，页面却列 10 项。原因是校验 1、2 在主流程里靠提前返回把关，
从未进入 checks 数组。修复：走到校验阶段就补记它们，计数与页面一致。

**⑤ 月度日历刷新在 31 号运行会跳月** — 已修复

`Date.setUTCMonth` 加月会溢出：`2026-01-31` 加一个月得到 `2026-03-03`，
整个二月被跳过，还会产生重复月份（实测 `01 03 03 05 05`）。
目前 cron 设在每月 1 号所以不触发，但手动运行就会踩到。
修复：改用年月整数推算，已验证跨年正确。

### P3 · 长期隐患

**⑥ 跨年时「迟到执行」判定会漏报** — 已修复

`late` 判定只查当年日历。12 月出信号、次年 1 月才执行时，
落在上一年的交易日不会被计入，漏跑一天也不会告警。
修复：信号日跨年时一并载入上一年日历。

**⑦ `/health` 无鉴权且其异常会触发推送** — 已修复

任何知道 Worker 地址的人反复请求 `/health`，在 GitHub 不可用时就能让你的手机刷屏。
修复：`/health` 单独捕获异常返回 503，不再发推送。端点本身保持公开（它只返回
仓库里本就公开的信息，无泄漏）。

**⑧ 每交易日全量重写 `series.json`（39.6 KB）** — 评估后接受

未压缩年增约 9.4 MB，但 git 对这种逐日追加的文件 delta 压缩效果很好，
当前 `.git` 仅 605 KB。十年内不会成为问题，不值得为此增加分片复杂度。

### 通过项（有证据的正面结论）

| 项目 | 证据 |
|---|---|
| 密钥零泄漏 | 全仓库检索 `github_pat_` / `BARK_KEY` / `RUN_TOKEN` / `device_key`，命中 0 |
| 金额零泄漏 | 遍历 `data/` `audit/` `calendar/` 全部 JSON，无任何金额字段 |
| 提交原子性 | Git Data API 分步执行，ref 更新前失败只留孤儿 blob，仓库状态不变 |
| 校验失败确实不写入 | 账本变更只在内存中，早退时整体丢弃，下次重跑会重新执行且标记迟到 |
| 日历缺失能自愈 | 当年日历没有今天时，当场向深交所查询并就地补齐 |
| 数值核心正确 | 与独立 Python 回测引擎逐笔对账（3 段历史、最多 62 笔路径，误差 < 0.01 元） |
| 计算器方向不变量 | 穷举 2814 组，方向错／金额越界／档位非法 0 组 |
| 补齐落点精确 | 穷举 2525 组，成交后仓位偏离目标 < 3.3e-16 |
| 提示边界正确 | 穷举 1205 组，0 档不提卖出、4 档不提买入，0 违规 |
| 交易日推算正确 | 2026 全年 242 天遍历，跨周末／跨长假／跨年全部正确 |
| 每项校验真能拦住问题 | 逐项故障注入，7 项全部抓到它声称拦的问题 |
| MA30 计算准确 | 独立重抓重算 138 个交易日，零处不一致 |

### 回归测试覆盖

`npm test` 现在覆盖：与 Python 引擎对账、信号阈值边界、档位推进、目标市值法公式、
MA30 边界、北京日期换算、交易日推算、计算器不变量穷举、补齐落点穷举、
提示边界穷举、**校验体系逐项故障注入**、前端模块版本戳。

改动 `shared/strategy.js` 后必须跑 `npm run stamp` + `npm test`，忘了 stamp 测试会直接失败。


---

## 追加审查：落后状态下的页面一致性（2026-09-10）

**场景**：T 日出信号，你 T+3 日才打开页面，在计算器里如实填入持仓。

**发现**：系统提供了正确信息（落后告警框 + 交易记录），但**页面最显眼的三处都在说相反的话**：

| 位置 | 落后时显示 | 问题 |
|---|---|---|
| 顶部大字 | 「距离下一次卖出，还需涨 0.26%」 | 按账本档位推演，而你空仓根本没得卖 |
| 计算器主结果 | 「已空仓，无可卖出」 | 等于说什么都不用做 |
| 时点提示 | 「今天不需要下单」 | 最危险 —— 你明明欠着 50 万买入 |

一个着急的人很可能只看顶部大字就关掉页面了。

**修复**：新增 `missedEntries()` 精确推断欠哪几笔（从账本末尾往回找起始档位等于实盘档位的那笔），
落后时三处统一切换为补做指引：

- 顶部：「你有 2 笔操作还没做，需买入 499,989 元」+ 列出漏掉的具体日期与档位变化
- 计算器：主位显示「补齐 2 档　买入 499,989 元　约 342,400 股」，副行附上按节奏走一档的金额
- 时点提示：「⚠ 你还欠着 2 笔操作，先补掉再等下一次信号」
- 原落后告警框在此状态下隐藏，避免重复且指代错乱

只有你在计算器里真的填过数字才判定落后 —— 默认值就是账本推算值，不会误报。

另修：次年日历改为临近年底才请求（平时必然 404，白发请求还在控制台留红字）。

---

## 追加审查：计算器输入的响应性与可逆性（2026-09-10）

**问题**：填错数字时提示会不会全部跳出来？填对之后会不会自动消失？

**实测发现两处：**

1. **顶部大字不跟随输入更新** —— `onCalcInput()` 只调 `calc()`，而顶部卡片在 `render()` 里渲染，
   两条路径没打通。填错数字时计算器和时点提示立刻切到补做态，**顶部却纹丝不动**，
   只有刷新页面才同步。这正是「页面自相矛盾」的问题在输入路径上又漏了一次。

2. **逐字输入会让红色告警闪烁** —— 输「500000」打到「5」时持仓占比几乎为 0，
   瞬间判定落后，红色告警闪一下又消失。

**修复**：把顶部卡片渲染抽成 `renderNext()`，输入路径一并刷新；
整个计算加 350ms 防抖，等你停手再判定。

**实测结果**：

| 阶段 | 顶部大字 | 计算器 | 时点提示 |
|---|---|---|---|
| 刚打开 | 距离下一次卖出，涨 0.26% | 卖出第2份 250,003 元 | 尚未触发 |
| 填错（持仓填 0） | **你有 2 笔操作还没做，需买入 499,989 元** | 补齐 2 档 买入 499,989 元 | ⚠ 欠着 2 笔 |
| 改回正确 | 距离下一次卖出，涨 0.26% | 卖出第2份 250,003 元 | 尚未触发 |

三处同步切换、同步恢复。逐字输入 6 个字符全程顶部文案零变化，停手后一次性落到正确状态。

---

## 追加审查：计算器隔离性与「金额」的真实含义（2026-09-10）

**问题**：计算器里乱填会不会污染交易记录？「金额」列是怎么算的？能不能照着它下单？

**隔离性 — 实测通过。** 四组极端输入（999万/0、1元/1元、30万/70万、0/500万），
交易记录金额与横幅金额**一次都没变**，只有计算器跟着变。

**「当时理论金额」的推导** — 从本金出发按账本逐日重放，逐日核对如下：

```
2026-09-01  总资产 1,000,000（本金）
2026-09-07  总资产 1,000,748（债券指数带涨）
2026-09-08  买入 250,156 → 1/5 档   ← 0.25×1,000,636 ÷ (1+0.25×0.000045)
2026-09-09  买入 248,738 → 2/5 档
```

它只依赖 本金 + 账本 + 行情，**是事后推导的历史值，不是下单指令**。

**又发现两个真 bug：**

**① 横幅金额也是理论值，实盘对不上时会让你买错量**

推送和横幅里的金额都来自本金重放，不是你的真实账户。实盘 30万现金/70万持仓时，
横幅显示 251,674 元，而按真实数字应为 49,998 元 —— 差 5 倍。
修复：横幅在实盘与理论差异超过 2% 时，补一行「按你填的实盘数字应为 X 元 —— 以这个为准」。

**② 有挂单时计算器多算了整整一档**

挂单目标是 3/5 档，实盘持仓 70% 已经基本到位，只需补 49,998 元。
但计算器按「从推断的 3 档再走一档到 4 档」算，给出 299,987 元 —— **多算 25 万**。
根因：`calcStep` 只会走一档，不看挂单的目标档位。
修复：有挂单时改用 `calcCatchUp(cash, hold, pending.tierTo)`，直接调到挂单那一档。
已加回归测试钉住这个语义，并验证成交后仓位精确落在 75.0000%。

**③ 横幅同样没跟随输入刷新**（第三次同类问题）

修复方式换成结构性的：新增 `renderInputDependent()` 收拢所有依赖计算器输入的渲染
（顶部卡片 + 横幅 + 计算器），输入路径和加载路径都只调它。
以后新增依赖输入的元素往这里加，不要再在 `onCalcInput` 里单独调 —— 这是前三次漏刷的共同根源。

**结论：交易记录是金标准（不受任何输入影响），但它的「金额」列是历史理论值，不能照着下单。
真要下单看计算器，那里用的是你填的真实数字。**

---

## 追加改动：金额降级为估算（2026-09-10）

**起因**：既然真金白银下单以计算器为准，推送和横幅里把理论金额做成主体就有误导性。

**改法**（推送、横幅、「下一次操作」三处统一）：

- 主体只说 **「买入第 N 份　目标仓位 75%」** —— 这两个数永远准确，不受实盘偏差影响
- 金额降为副行小字，明确标注 **「估算约 X 元（按本金推算，下单以面板计算器为准）」**
- 实盘与理论差超过 2% 时，副行补一行 **「按你填的实盘数字应为 X 元 —— 以这个为准」**

**顺带修的一个漏洞**：那句实盘校正原本只挂在横幅上，**点过「已完成」横幅收起后就消失了**，
只剩理论估算。现在「下一次操作」卡片里也有一份，横幅收起也看得见。

**为什么「第 N 份」不损失信息**：每一份差不多就是总资产的 25%（0→25%、25→50%…每步都是
~25%），所以知道自己本金的人一看就有量级概念。

---

# 第二轮全面审查（2026-09-10 夜）

上一轮之后改动很大（ETF 多源、推送送达状态、校验重写、落后态、计算器目标、金额降级、
`render()` 重写），需要重新验一遍。这轮换了更硬的方法：**穷举状态空间自动查不变量**，而不是抽查。

## 逻辑层：2808 组穷举

`npm run sweep` —— 5 档位 × 8 价格位置 × 3 挂单形态 × 3 资产规模 × 9 持仓占比。

检查的不变量：0 档不提卖出、满档不提买入、任何档位都有可执行方向、距离非负、
金额有限且不超总资产、方向合法、**有挂单时成交后仓位精确落在挂单目标上**、
股数是 100 的整数倍、补齐后落点精确。

结果：**违反 0 组。**

## 显示层：6 种代表性状态

| 状态 | 结果 |
|---|---|
| 0 档 · 无挂单 · 价高于卖出线（今天的真实状态） | ✓ |
| 0 档 · 已触发买入 | ✓ |
| 2 档 · 已触发卖出 | ✓ |
| 满仓 · 价已跌破买入线（不该提买入） | ✓ 正确改说距卖出 |
| 2 档 · 价在中间（两方向都可能） | ✓ 取了更近的（距卖 2.00% < 距买 3.00%） |
| 0 档 · 未设本金 | ✓ 优雅降级 |

每种自动检查：可见文本无 undefined／NaN、档位与格子数一致、顶部与横幅与计算器方向一致、
挂单存在性与横幅显示一致、所有金额有限非负。**违例 0。**

## 生产代码实测

- **ETF 多源（P1 修复）**：Worker 里那份实现走真实网络，515180 取到 1.46（腾讯），
  深市代码 159545 取到 1.315 —— 沪深前缀两条路径都对。
- **交互回归**（`render()` 重写后）：七档时间尺度全部正常渲染、图表按下显示抬起收起、
  本金设置/取消/完成三条路径都对。
- **深色模式**（此前从未验证）：对比度 0.91 vs 0.08，图表颜色已解析成具体值，
  切换主题时图表会重绘（深色 `#1A1F27…` → 浅色 `#1B2431…`）。

## 本轮发现的问题

**状态文件损坏时，页面会硬着头皮画出垃圾** —— 已修复

把 `state.json` 的 `tier` 改成 7 之后：档位显示 7、格子亮满 5 个（看着像满仓）、
计算器却说"已空仓无可卖出"、页面上出现 NaN（`WEIGHTS[6]` 不存在）—— **全程没有任何提示**。

一个看似正常的错数字，比一屏红字危险得多。

修复：新增 `validateState()`，检查档位范围与整数性、日期格式、指数字段为正数、
买入线低于卖出线、挂单档位合法且只跨一档、挂单起始档与当前档位吻合。
前端校验不过就整页停止渲染并列出原因；Worker 读到损坏状态直接告警退出，不基于它算信号。
12 种损坏形态已纳入回归测试，完好状态无误报。

## 新增的两个运维命令

| 命令 | 作用 |
|---|---|
| `npm run sweep` | 状态空间穷举，改完策略逻辑后跑 |
| `npm run check:sources` | 三个外部数据源体检 —— 这轮最有价值的发现就是数据源会悄悄失效 |

## 尚未在生产验证的部分

Worker 的新代码（ETF 多源、校验 1/2 补记、校验 9 重写、推送送达状态、状态校验）
**还没在真实运行里跑过** —— 今晚 21:00 那次用的是旧代码。
建议手动触发一次 `/run?token=…&force=1` 提前验证，不要等到下一个交易日才发现问题。

---

# 第三轮：文案专项审查（「下一次操作」与推送）

只查一件事：每一种状态是否都有话说，每一句话是否准确、不会被误解。
共查出 8 处，其中 2 处会直接把人引到错误的下单日或错误的方向。

## 会造成实际损失的两处

**1. 推送「明日收盘执行」在周五和节前是错的** —— 已修复

`pushDaily` 把执行日写死成「明日」。实际执行日是信号日的下一个**交易日**：
周五出的信号要到下周一，春节前的信号能差 10 天。日历里 241 个信号日中，
**50 个（21%）的执行日不是第二天**。照字面理解会在休市日空等一个不会来的成交，
或者以为错过了而在错误的日子补单。

修复：`runDaily` 用交易日历算出 `execDay` 传给推送；新增 `execWording()`，
只有执行日确实是第二天才说「明日」，否则把日期和星期写出来
（「9 月 14 日收盘执行」／正文「执行日 2026-09-14（周一）收盘前，中间的休市日不用操作」）。
12 月底的信号会落到明年，当年日历取不到下一天时自动去查次年日历。
提交信息里的「→ 明日执行」一并改成真实日期。

**2. 横幅和顶部卡片在执行日当天互相矛盾** —— 已修复

横幅顶行硬编码「触发 · 下一个交易日收盘执行」，从不调用 `execInfo()`。
到了 T+1 当天，顶部卡片说「今天收盘前买入第 2 份」，横幅仍说「下一个交易日收盘执行」。
横幅是整屏最显眼的元素，两句话正面打架，只会让人不敢下单。

修复：横幅顶行改用 `execInfo().kick`，时点跟着日期走。
回归测试里加了断言：卡片说「今天」而横幅不说「今天」直接判失败。

## 其余六处

| # | 问题 | 修复 |
|---|---|---|
| 3 | 执行日已过时拼出「9 月 11 日 收盘（执行日已过）买入第 3 份」，句子断裂 | `execInfo` 改为返回可直接拼接的 `lead`，四种时点分别给词（今天／具体日期＋星期／本应在…／算不出）|
| 4 | 实盘**高于**账本时仍说「你有 N 笔操作还没做」，方向说反 | 副行按高低分叉：「实盘 2/5 档，**高于**账本的 0/5 档」|
| 5 | 说「3 笔操作还没做」却只给一个金额（账本走过 0→1→2→1 时净档差只有 1 档）| 金额改为「需**一次性**买入 X 元**补到位**」，并注明「3 笔合起来只需这一次调仓」|
| 6 | 没有对得上的漏做记录时，凭空按档差编出「1 笔操作还没做」 | 改为「你填的实盘仓位和账本对不上」，并说明可能是持仓涨跌跨过档位边界或计算器数字填错 |
| 7 | 「等今日收盘确认」「还需跌 5.35%」都是按 `asof` 收盘价算的，却写得像实时价 | 全部锚定到日期：「2026-09-10 收盘 5,760 已在买入线 5,820 之下」「百分比按 2026-09-10 收盘 12,305 计」|
| 8 | 1–3 档时买卖两条线同时有效，卡片只报更近的一个，读起来像方向已定 | 新增 `otherSide()`：「反向也可能先到：跌 3.00% 到 5,820 则改为买入第 3 份。」|

顺带修掉两处一致性问题：
顶部卡片的买卖方向此前直接取 `pending.side`，而横幅和推送取的是目标市值公式算出来的方向 ——
极端行情下两处会说反，现在统一以公式为准。
无挂单推送在当天刚成交过时标题仍是「无操作」，现在分成
「✅ 今日已成交　N/5 档　明日无需操作」和「红利MA30 · 明日无需操作　N/5 档」。
同一条推送里的距离文案原本用 `Math.abs()` 把负数翻正，已破线时会谎报「还需跌 X%」，
现在直说「已跌破买入线」「已满仓，不再加仓」「当前空仓，没有可卖的份额」。

## 新增：文案回归测试 `npm run wording`

光靠眼睛看不住文案 —— 这轮的两个大问题都是**在特定日期才会显形**的。
新增 `tests/wording.mjs`，已并入 `npm test`：

它把 `index.html` 里的 `<script type="module">` 整段抠出来，配一套极简 DOM 假件后
import 进来跑**真代码**（不是复刻一份逻辑，所以以后改了渲染函数测试会跟着变），
再穷举 15 种状态打印出真实生成的句子，并对以下写法直接判失败：

- 文案里漏出 `undefined` / `NaN` / `null` / `[object`
- 卡片说「今天收盘前」而横幅说别的时点
- 卡片说买入而横幅说卖出
- 说了「N 笔」但账本里对得上的不是 N 笔
- 没有漏做记录却说「N 笔操作还没做」
- 实盘高于账本却让你买入（或反之）
- 主行说买入而副行说「低于账本」对不上

推送那侧则拿 2026 年全部 241 个信号日逐个跑 `execWording()`，
断言「明日」二字**当且仅当**执行日真是第二天时才出现，否则必须写出具体日期。

造场景时账本和档位必须自洽（tier 3 就得真有 0→1→2→3 三笔），
否则重放出来的持仓和档位矛盾，测出来的方向也是假的 —— 第一版就栽在这里，
把一个不可能出现的状态测出了「档位 3/5 → 2/5，买入第 2 份」。

## 附带补上：线上版本从外面看不见

这轮结束后推代码，想确认「改动到底部署了没有」，只能去翻 Cloudflare 的构建记录 ——
而**构建成功不等于你想要的那版真的在跑**，控制台也答不了「此刻在跑哪一版」。

修复：`worker/src/index.js` 里加了 `const BUILD`，由 `npm run stamp` 按
worker 源码 + 核心模块的联合指纹算出并写回，`/health` 原样返回。
算指纹前会先把 `BUILD` 那一行本身抹平 —— 否则写回的动作改变文件内容，
下次算出来又是另一个值，永远盖不稳（已验证连跑三次结果相同）。

配套 `npm run check:deploy -- <worker 地址>`：本地算一遍，请求线上比一遍，
顺带把数据截至日、档位、挂单、上次运行的校验结果和推送状态一起打出来。
忘了盖章的情况由 `npm test` 拦截（故意盖错章验证过会报 `总判定：有不一致 ✗`）。

Worker 地址**不写进仓库**：仓库是公开的，而 `/health` 不鉴权，
写进去等于把一个免鉴权端点挂到搜索引擎上。

---

# 第四轮：所有时间措辞改成具体日期

第三轮只修了推送里的「明日」。这轮把范围拉到全系统 —— 面板、推送、
告警、校验项名称，凡是相对时间词一律改成具体日期。共改 30 余处。

## 为什么值得全改

三种相对词，三种错法：

| 写法 | 什么时候是错的 |
|---|---|
| 明日／次日 | 周五出的信号执行日是下周一，长假前差十天。241 个信号日里 50 个不是第二天 |
| 今天 | 手机上放了一夜的页面，它说的「今天」是昨天。面板上的校验项名叫「交易日历确认**今天**开市」，可它显示的是**上一次运行**的结果 |
| 下一个交易日 | 不翻日历根本不知道是哪天 |

第三种最隐蔽：它听起来很严谨，却什么也没说。

## 立的规矩

**凡出现相对时间词的那一句，要么同句给出具体日期，要么明说「给不出具体日期」。**
相对词只许待在日期后面的括号里做定位 —— 把括号整个删掉，句子必须依然完全正确。

措辞统一由 `shared/strategy.js` 新增的 `dayLabel(日期, 北京今天)` 生成：

```
dayLabel('2026-09-14', '2026-09-11').label   // '9 月 14 日（周一）'
dayLabel('2026-09-11', '2026-09-11').label   // '9 月 11 日（今天，周五）'
```

推送和面板共用同一个函数 —— 之前 Worker 里有一份 `weekdayCN`、
前端有一份 `wd`，各写各的迟早会分家。

## 改到的地方

- **执行日**：`execWording`（推送）与 `execInfo`（面板）全部改为日期打头。
  横幅顶行、卡片主行、计算器时点提示三处措辞现在同源。
- **无挂单推送**：标题从「明日无需操作」改成「9 月 11 日无操作」，
  正文补一行「9 月 14 日（周一）收盘前无需操作」——
  周五发出的「明日无需操作」，字面上说的是周六。
- **已成交推送**：「✅ 今日已成交」→「✅ 9 月 11 日已成交」，成交行带上星期。
- **盘中提醒**：「⏰ 今天收盘前买入第 N 份」→「⏰ 9 月 11 日收盘前买入第 N 份」。
- **告警**：「今日数据未到」→「9 月 11 日数据未到」；
  「明年日历尚未发布」→「2027 年日历尚未发布」；
  迟到告警现在写出信号日和实际成交日。
- **校验项名称**：「交易日历确认今天开市」→「运行日在交易日历内」，
  「中证今日数据已到位」→「中证运行日数据已到位」。面板显示的是上次运行的结果，
  名字里不能有「今天」。Worker 和前端两处映射同步改。
- **等待触发时**：执行日在触发前根本算不出来，于是直说
  「触发当日 21:00 挂单，再下一个交易日收盘执行 —— 具体日期要到触发后才定得下来」。
- **百分比口径**：「百分比按 2026-09-10（周四）收盘 6,000 计」，把星期也带上。

## 顺手修掉的一个漏网 bug

「每份该是多少钱」板块里的 `renderTiming` 还留着
`behind.missed.length || Math.abs(behind.gap)` —— 第三轮在顶部卡片修掉的
「没有漏做记录却凭空说 N 笔」，这里当时漏了。两处现在一致。

## 排版

手机上「9 月 11 日（今天，周」/「五）」会从中间断开，
一个被劈成两半的日期比没有日期还难认。日期整段裹 `.nb{white-space:nowrap}`；
`lead`/`kick` 保持纯文本（要喂给 `textContent`），另出一份 `leadHtml` 带标签。

## 测试

`npm run wording` 现在做三件事：

1. 15 种状态跑真代码，逐句检查相对词旁边有没有日期；
2. 2026 年 241 个信号日逐个验 `execWording`：具体日期必须出现在标题里，
   括注里的「明天」当且仅当执行日真是第二天时才准出现；
3. **静态扫源码** —— 上面那些告警分支平时跑不到，而它们恰恰最容易留下
   「今日」「明日」，出事那天才亮相。扫 `worker/src/index.js` 和 `index.html`
   的字符串字面量，写死的相对词一律揪出来（注入一处「明日」验证过会报错）。

`dayLabel` 本身在 `npm test` 里另有覆盖：跨月、跨年、国庆前后各一例，
再遍历前后各 400 天断言「相对词只出现在相差 1 天以内」。

## 补记：仓位不再用分数表示

使用者问「0 到 5 档分别对应多少钱」—— 而根本没有 5 档。
文档 3.3 写的是**五个档位**（0–4），满仓是第 4 个，
面板却显示成「4/5 档」，读起来像还差一档。

「当前档位」那张卡片更直接：画 5 个圆点、按 `k < tier` 点亮，
**满仓时永远剩一个暗点**，视觉上明确地说「还能再买一次」——
而 `canBuy = tier < MAX_TIER`，到 4 就买不动了。

顺带查出一处自相矛盾：计算器里满仓写的是「相当于 **5/5** 档」，
而别处满仓写「4/5 档」，同一个状态两种写法。

**改法（使用者选定）**：位置一律用仓位百分比，分数全部去掉。
「档」这个字只保留给**步数**（「差 2 档」「只走一档」），那本来不会误读。
措辞由 `shared/strategy.js` 的 `posPct(tier)` 统一给出，面板 23 处、
Worker 6 处、`check-deploy` 1 处全部转换；圆点改成 4 个，满仓全亮。

**注意**：本文档前面几轮的记录里仍然写着「N/5 档」——
那些是当时代码的真实样子，属于历史记录，不做改写。

### 这一处是靠人读出来的，不是测试抓出来的

前面几轮写了大量断言，一条都没碰到它。原因很直接：
**测试只能验「代码有没有按我写的规则跑」，验不了「规则本身会不会被人误解」。**
所以补了两道能覆盖这类问题的防线：

1. `fractionsIn()` 运行时断言 —— 对**真正渲染出来的文本**检查，
   顶部卡片主行与副行、横幅三行、计算器三行、交易记录的仓位列，
   一律不许出现 N/M 形式（「校验 7/8」那种通过计数是合法分数，不在此列）。
2. 仓位卡片回归 —— 断言五档的显示文本、圆点必须 4 个、
   亮灯数等于档位、**满仓不许有暗点**。

注入验证时还发现 `calculator.mjs` 自己有盲区：它没抓 `nextSub`，
把「N/5 档」注入回顶部卡片副行它一处都报不出来。补上之后
注入必报（wording 6 处、calculator 2 处）。

## 补记：「收盘」还是「收盘前」

改完日期之后，使用者问了一句「应该是收盘还是收盘前」，翻出一处真问题：
系统当时两种说法都在用 —— 推送标题「收盘执行」，正文和面板「收盘前」。

我第一版按说明文档统一成了「收盘成交」，理由是账本按当日收盘价记账，
「收盘前」字面上允许上午十点就买。使用者反馈说这样读起来像
**「收盘之后再操作」** —— 而那是个做不到的动作，收盘之后市场已经关了。

这个反馈是对的，而且它暴露了我那版的漏洞：我只堵了「买太早」，
没意识到「收盘」会被读成「买太晚」。最终方案是两句一起上：

| 管什么 | 靠哪句 |
|---|---|
| 不能拖到收盘之后 | 「**收盘前**执行」 |
| 也不能买太早 | 「请在尾盘（14:50 之后、收盘之前）下单 —— 账本按当日收盘价记账，买太早会和账本对不上」 |

后一句就是 `shared/strategy.js` 里的 `CLOSE_TIP`，挂单推送和盘中提醒都必须带。
`npm run wording` 加了三道拦截：光秃秃的「收盘＋动词」、推送标题里缺「收盘前」、
给了执行时点却没带尾盘提示（三条都注入验证过会报错）。

**没有写进文案的一件事**：515180 是上交所 ETF，尾盘那几分钟究竟走连续竞价
还是收盘集合竞价，会影响「14:50 之后」的最佳下单时点。这一点我不确定，
所以文案只写了任何情况下都成立的「14:50 之后、收盘之前」，没有对交易机制下断言。
